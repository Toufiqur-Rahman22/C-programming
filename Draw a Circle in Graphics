#include <graphics.h>
#include <conio.h>

int main() {
    int gd = DETECT, gm;

    initgraph(&gd, &gm, "");

    circle(250, 200, 100);    // (x, y, radius)

    getch();
    closegraph();

    return 0;
}
