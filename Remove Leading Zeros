#include <stdio.h>
#include <string.h>

int main() {
    char s[200];
    int i = 0;

    printf("Enter number string: ");
    gets(s);

    while(s[i] == '0')
        i++;

    if(s[i] == '\0')
        printf("0");
    else
        printf("%s", s + i);

    return 0;
}
