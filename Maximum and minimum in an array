#include <stdio.h>

int main() {
    int a[6] = {5, 2, 8, 11, 1, 4};
    int max = a[0], min = a[0], i;

    for(i = 1; i < 6; i++) {
        if(a[i] > max) max = a[i];
        if(a[i] < min) min = a[i];
    }

    printf("Max = %d\nMin = %d", max, min);
    return 0;
}
