#include <stdio.h>

int main() {
    int a[5] = {10, 25, 3, 99, 4};
    int max = a[0], i;

    for(i = 1; i < 5; i++) {
        if(a[i] > max)
            max = a[i];
    }

    printf("Largest element = %d", max);
    return 0;
}
