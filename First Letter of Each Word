#include <stdio.h>

int main() {
    char str[200];
    printf("Enter string: ");
    gets(str);

    if(str[0] != ' ')
        printf("%c ", str[0]);

    for(int i = 1; str[i]; i++)
        if(str[i] != ' ' && str[i-1] == ' ')
            printf("%c ", str[i]);

    return 0;
}
