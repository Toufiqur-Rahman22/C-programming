#include <stdio.h>
int main() {
    int n, val = 0;
    printf("Enter rows: ");
    scanf("%d", &n);

    val = n * (n + 1) / 2;

    for (int i = n; i >= 1; i--) {
        for (int j = 1; j <= i; j++)
            printf("%d ", val--);
        printf("\n");
    }

    return 0;
}
